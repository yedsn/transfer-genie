# 变更：托盘菜单、选择模式与清理选项

## 为什么
用户需要在托盘菜单中禁用全局快捷键、在选择模式里快速全选，并控制旧数据的清理方式（时间范围与清理范围）。

## 变更内容
- 在托盘菜单中加入启用/禁用全局快捷键的切换项。
- 在消息选择模式中加入“全选”操作。
- 扩展手动清理，支持时间范围（全部、7天以前）与清理范围（仅本地、包含 WebDAV 远端）。

## 影响
- 影响规格：app-shell、message-feed、client-settings
- 影响代码：托盘菜单与快捷键注册、消息选择 UI、针对本地/WebDAV 的清理逻辑
