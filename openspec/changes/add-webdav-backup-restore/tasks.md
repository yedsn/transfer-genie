# WebDAV 备份与恢复任务

-   [x] 1.0 **UI:** 在设置界面添加“备份 WebDAV”和“恢复 WebDAV”按钮。
-   [x] 2.0 **后端:** 实现 `backup_webdav` 命令。
    -   [x] 2.1 获取 `history.json`。
    -   [x] 2.2 列出 `files/` 目录中的所有文件。
    -   [x] 2.3 在内存中创建一个 zip 压缩包。
    -   [x] 2.4 将 `history.json` 添加到压缩包。
    -   [x] 2.5 从 `files/` 下载每个文件并将其添加到压缩包。
    -   [x] 2.6 提示用户保存生成的 zip 文件。
-   [x] 3.0 **后端:** 实现 `restore_webdav` 命令。
    -   [x] 3.1 提示用户选择一个 zip 文件。
    -   [x] 3.2 添加一个确认对话框，警告数据将被覆盖。
    -   [x] 3.3 在内存中解压压缩包。
    -   [x] 3.4 验证 zip 包中是否存在 `history.json` 和 `files/` 目录。
    -   [x] 3.5 删除远程 `files/` 目录中的所有现有文件。
    -   [x] 3.6 上传新的 `history.json`。
    -   [x] 3.7 将所有文件上传到 `files/` 目录。
-   [x] 4.0 **集成与测试:**
    -   [x] 4.1 将 UI 按钮连接到后端命令。
    -   [x] 4.2 手动测试备份和恢复功能。
    -   [x] 4.3 确保 UI 在备份/恢复过程中提供反馈（例如，加载指示器、成功/错误消息）。
