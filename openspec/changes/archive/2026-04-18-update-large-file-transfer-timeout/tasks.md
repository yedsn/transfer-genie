## 1. Implementation
- [x] 1.1 拆分 WebDAV 短请求与流式上传请求的超时策略，移除上传路径上的固定 30 秒总超时
- [x] 1.2 为 WebDAV 文件上传增加基于文件句柄或临时文件的流式发送辅助方法，并保留 `Content-Length` 与进度回调
- [x] 1.3 重构桌面端 `send_file` 路径，避免在上传前通过 `fs::read` 将整文件读入内存
- [x] 1.4 重构本机 HTTP 文件发送入口，避免通过 `field.bytes()` 将整文件读入内存，并复用统一上传管线
- [x] 1.5 校准上传完成、失败、缓存落盘、消息索引与上传历史之间的先后顺序，确保失败传输不会被记录为成功
- [x] 1.6 增加覆盖长时上传与大文件场景的回归测试，包括持续传输超过 30 秒仍可成功的用例
