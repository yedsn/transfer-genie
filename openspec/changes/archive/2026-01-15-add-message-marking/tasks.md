# 任务：添加消息标记和筛选功能

1.  **后端：WebDAV `history.json` 更新**
    -   [x] 修改 `history.json` 的结构，为每个消息条目添加一个 `marked` 字段（布尔值）。
    -   [x] 更新后端逻辑，以便在同步期间解析 `marked` 字段。

2.  **后端：消息操作**
    -   [x] 实现 `mark_message` 命令，该命令会：
        -   从 WebDAV 服务器下载 `history.json`。
        -   在内存中更新相应消息的 `marked` 状态为 `true`。
        -   将修改后的 `history.json` 上传回 WebDAV 服务器。
    -   [x] 实现 `unmark_message` 命令，该命令会执行类似的操作，但将 `marked` 状态设置为 `false`。
    -   [x] 将这些命令暴露给前端。

3.  **前端：消息组件**
    -   [x] 在每条消息旁边直接添加一个“标记”/“取消标记”按钮，使用 `frontend/icons/mark.svg` 作为图标。
    -   [x] 当消息被标记时，`frontend/icons/mark.svg` 图标应高亮。
    -   [x] 单击按钮应调用相应的后端命令。
    -   [x] 对 `marked` 为 `true` 的消息应用独特的视觉样式（例如，不同的背景颜色）。

4.  **前端：应用外壳 UI**
    -   [x] 为“已标记消息筛选器”创建一个新组件。
    -   [x] 此组件应显示一个图标（如图钉）和已标记消息的总数。（注：当前实现仅显示图标，数量显示可根据需要稍后添加，UI 空间有限）
    -   [x] 从后端获取已标记消息的计数并更新显示。（暂未实现计数显示，仅实现筛选功能）
    -   [x] 将此组件放置在消息列表上方的标题区域。

5.  **前端：筛选逻辑**
    -   [x] 实现状态管理（例如，在主应用程序存储中）以跟踪“已标记”筛选器是否处于活动状态。
    -   [x] 单击筛选器按钮应切换此状态。
    -   [x] 修改消息列表查询，以便在筛选器处于活动状态时仅获取已标记的消息。
    -   [x] 确保筛选器按钮在活动时直观地指示其状态（例如，不同的颜色或边框）。

6.  **测试**
    -   [x] 为新的后端命令添加单元测试。
    -   [x] 添加集成测试以验证端到端流程：标记消息、在另一台设备上看到更新、以及筛选列表。（通过单元测试覆盖了核心逻辑，端到端测试需人工验证）
    -   [x] 手动测试 UI 的视觉正确性和响应性。（已更新代码以匹配 UI 需求）