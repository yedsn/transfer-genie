# 提案：更新文件上传选择机制

当前的文件上传机制在选择文件后会立即发送。本提案旨在通过允许多文件选择并将发送操作推迟到用户明确点击“发送”按钮后，来增强用户体验。

当用户点击“附件”图标时，他们应该能够选择多个文件。选择后，这些文件应显示在消息撰写区域的文本输入框下方列表中。列表中的每个文件都应有一个按钮，用于将其从选择中移除。

主“发送”按钮将用于发送文本消息和所有选定的文件。这些文件将按顺序上传，并且界面将显示每个文件的上传进度。