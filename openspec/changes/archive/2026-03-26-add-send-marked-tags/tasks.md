## 1. 实现
- [x] 1.1 在首页输入区增加独立“标记”勾选和悬停弹出的标签管理层 UI。
- [x] 1.2 支持在弹出层中为本次发送暂存选择标签、新增标签、删除标签。
- [x] 1.3 扩展 `send_text`、`send_file` 和 `send_file_data`，支持接收发送前标记状态和暂存标签变更，并在发送成功时一起持久化。
- [x] 1.4 在发送成功后清空发送前暂存状态，在发送失败时保留当前状态。
- [x] 1.5 验证文本发送、文件发送、仅标记不选标签发送、未勾选标记发送四条路径仍然正常。
