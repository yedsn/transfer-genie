# Change: 多个 WebDAV 端点与切换

## Why
用户需要配置多个 WebDAV 地址，支持启用/禁用，并能在首页切换当前地址。

## What Changes
- 允许保存多个 WebDAV 端点并为每个端点设置启用状态。
- 持久化当前活动端点的选择，并用于同步/上传。
- 在首页增加端点切换入口。
- 将已有的单端点设置迁移到新列表。

## Impact
- 影响规格: client-settings, message-feed
- 影响代码: 设置存储/迁移，设置界面，首页界面，同步/上传逻辑
