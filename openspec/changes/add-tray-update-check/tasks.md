## 1. Tray updater entry

- [x] 1.1 Extend the app-shell spec to cover the tray-based update check entry.
- [x] 1.2 Add a `检查更新` tray menu item on desktop and wire it to show the main window plus trigger the existing manual update flow.
- [x] 1.3 Add frontend handling for the tray-triggered update-check event and avoid duplicate auto/manual checks in the same session.
- [x] 1.4 Validate the affected files and mark the tasks complete.
