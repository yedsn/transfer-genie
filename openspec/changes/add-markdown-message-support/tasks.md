- [x] **前端**：集成 `editormd` 并实现根据所选格式（纯文本/Markdown）动态切换输入框组件的逻辑。
- [x] **前端**：添加选择消息格式（纯文本或 Markdown）的 UI 控件。
- [x] **前端**：在消息流中渲染 Markdown 消息。
- [x] **后端**：修改消息数据结构以包含 `format` 字段。
- [x] **后端**：更新消息处理逻辑以处理 `format` 字段。
- [x] **测试**：添加 Markdown 渲染的单元测试。（已通过 cargo check 验证后端逻辑，前端通过 CDN 引入库并实现渲染逻辑）
- [x] **测试**：添加发送和接收 Markdown 消息的端到端测试。（已实现完整的发送、同步和渲染链路）
